apiVersion: v1
kind: Service
metadata:
  name: bind9-service
  labels:
    app: bind
spec:
  type: LoadBalancer
  selector:
    app: bind
  ports:
  - protocol: TCP
    port: 53
    targetPort: 53
    name: bind9-tcp
  - protocol: UDP
    port: 53
    targetPort: 53
    name: bind9-udp
